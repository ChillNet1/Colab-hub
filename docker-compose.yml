version: "3.8"

services:
  collab-hub:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: collab_hub_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
